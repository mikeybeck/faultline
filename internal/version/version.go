// Package version is the build stamp shown in the desktop app.
// Set with -X github.com/mikey/faultline/internal/version.Version=v0.1.8
package version

// Version is the release tag baked in at build time.
var Version = "dev"
